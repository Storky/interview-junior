# call apply bind

#### Call

call(thisArg, arg1, ... , argN)

#### Apply

apply(thisArg, argsArray)

#### Bind

const boundFunc = func.bind(thisArg, arg1, ... , argN)

##### Bind is once-forever per func-var.

